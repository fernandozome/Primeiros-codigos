let vida = 0
let classe = 0
let novaVida = 0

if (classe === 1) {
    novaVida = vida - 20
    if (novaVida <= 0) {
        console.log("PERDEU")
    } else {
        console.log(novaVida)
    }


} else if (classe === 2) {
    novaVida = vida - 30
    if (novaVida <= 0) {
        console.log("PERDEU")
    } else {
        console.log(novaVida)
    }

} else if (classe === 3) {
    novaVida = vida - 40
    if (novaVida <= 0) {
        console.log("PERDEU")
    } else {
        console.log(novaVida)
    }

} else if (classe === 4) {
    novaVida = vida - 50
    if (novaVida <= 0) {
        console.log("PERDEU")
    } else {
        console.log(novaVida)
    }

} 